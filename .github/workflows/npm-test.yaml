---
name: npm-test
on: push
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4
      with:
        sparse-checkout: .
    - name: install
      run: npm install
    - name: test
      run: npm test
